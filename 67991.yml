---
# This playbook prints a simple debug message
- name: Echo 
  hosts: all
  gather_facts: yes
  become: yes
  become_user: root
  become_method: sudo
  connection: local

  tasks:
  - name: Print debug message
    debug:
      msg: Hello, world!
