---
- name: Print some debug information
  hosts: all
  vars: 
    files: "{{ windows_diruse.files.items()  }}"
  tasks:
    - name: Define variable
      debug:
        msg: "{{ files[devcon.exe] }} is an environment variable"
