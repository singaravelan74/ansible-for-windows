---
- name: Print some debug information
  hosts: all
  gather_facts: true
  tasks:
    - name: Print some jinja variable
      debug: 
        msg: "{{ ansible_hostname }}"       
      tags: debug_info 
